// Vakpagina (dummy content)
document.addEventListener('DOMContentLoaded', () => {
    const vakNaam = document.getElementById('vakNaam');
    vakNaam.textContent = 'Wiskunde 3A'; // later dynamisch
});
